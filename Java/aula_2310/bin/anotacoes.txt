variavel.charAt(0) -> vê a primeira letra da palavra digitada
variavel.lenght() -> mostra a quantidade de caracteres da palavra digitada
Character.digit(variavel, base)
charAt pega o símbolo e não o número, por isso para usar esse símbolo, tem que passar
para uma outra variável pegando o seu valor